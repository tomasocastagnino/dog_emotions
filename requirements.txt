tensorflow==2.19.0
keras==3.15.0
kagglehub==1.0.2
numpy==2.1.3
scikit-learn==1.9.0
opencv-python==4.13.0.92

# Solo para convertir modelos a TensorFlow.js (docs/model/). Instalarlo baja
# tensorflow (pedía 2.21.0 antes de instalar esto -- tensorflowjs fuerza <=2.19).
# Los .keras entrenados con 2.21 se siguen cargando bien en 2.19, verificado.
tensorflowjs==4.22.0
# tensorflowjs importa tensorflow_hub, que todavía usa pkg_resources -- setuptools
# >=81 lo saca. Sin este pin, `tensorflowjs_converter` explota con
# "ModuleNotFoundError: No module named 'pkg_resources'".
setuptools<81
